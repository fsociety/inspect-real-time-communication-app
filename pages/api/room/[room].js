export default function handler(req, res) {
    /*
    if (req.method === "POST") {
        // get message
        const { room } = req.query

        // dispatch to channel "message"
        res?.socket?.server?.io?.emit("room", room);

        // return message
        res.status(201).json(message);
    }
    res.end(`room: ${room}`)
    */
  }